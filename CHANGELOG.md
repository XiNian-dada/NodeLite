# 更新日志

本文件记录项目中的显著变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/en/1.0.0/)，
本项目遵循 [Semantic Versioning](https://semver.org/spec/v2.0.0.html)。

## [未发布]

## [v3.0.3-beta.2] - 2026-08-21

### 安全
- 升级 `lru` 至 0.18.2，修复 RustSec 报告的 panic 路径潜在 use-after-free。

## [v3.0.3-beta.1] - 2026-08-21

### 新增
- 设置页支持在密码或 2FA 二次确认后删除 Agent；删除会撤销注册与安装令牌、断开会话并立即从面板移除，历史数据保留。

## [v3.0.2] - 2026-08-11

### 新增
- 节点套餐支持按自然月统计流量、展示用量，并可在达到 95% 阈值后向 Linux Agent 下发双向限速。
- 设置页可填写节点信息并经密码或 2FA 确认后，直接生成一次性 Agent 安装命令。
- 地图上重叠的节点会合并为单一标记，悬停即可查看该位置的全部节点状态与指标。

### 变更
- 历史与审计写入器的批处理和刷盘间隔可配置，设置页新增 token 验证缓存诊断。
- 节点服务元数据的更新路径完成整理，便于同时维护续费信息和流量套餐配置。

### 修复
- Linux 流量限速规则使用专属 `tc` handle，撤销或更新限速时不会误操作同优先级的其他规则。

### 安全
- 更新前端构建工具链，纳入已修复的安全依赖版本。
- 生成安装命令的敏感响应禁止缓存，且不会记录一次性安装 token。

## [v3.0.2-rc.2] - 2026-07-12

### 新增
- 新增可配置的 token 验证并发限制和压力指标，便于控制认证风暴下的 CPU 与内存预算。
- 新增历史查询并发数、读取缓存预算等配置，并补充 Linux 内存矩阵与安装器默认值验证。
- 发布流程新增安装脚本校验和，版本化 Web 更新器会验证目标版本与 SHA256 清单。

### 变更
- 限制 Agent、浏览器 WebSocket 每连接的传输缓冲、消息尺寸、身份字段和慢发送时长，降低大规模连接的常驻内存。
- Agent 改用更精简的运行时与依赖 feature；历史查询改为按字节约束缓存、复用等待后的缓存结果并在查询路径外清理。
- 定义统一的 Server、Agent 与 systemd cgroup 内存测量方法，区分 RSS/PSS、匿名内存和可回收页缓存。

### 修复
- 修复 token refresh 宽限期、恢复 token 重试和取消验证等待的边界问题。
- 修复长磁盘挂载路径在桌面和移动端表格中的溢出。
- 修复不安全的 legacy 节点元数据迁移，并保留历史查询请求取消后的并发许可生命周期。

### 安全
- TOTP 时间步改为原子消费，防止同一步验证码被并发重复使用。
- Web 更新器校验目标版本和 SHA256 清单后再执行更新。

## [v3.0.2-rc.1] - 2026-07-05

### 新增
- 新增持久化 Agent 日志、日志查看页和最近登录通知。
- 节点详情新增 IP 显示开关，地图在高密度区域使用更小标记，并补充基于 fixture 的 Playwright E2E 流程。
- Release 新增 CycloneDX SBOM，运维文档补充 Prometheus、健康检查和 SQLite 可观测示例。

### 变更
- 浏览器实时更新改为服务端集中计算 diff 并广播，消除初始快照与订阅之间的竞态。
- GeoIP 字段引入字符串池，节点列表 API 使用更轻量的借用视图，减少大规模节点列表的克隆与分配。
- 收窄 Agent Tokio feature，并优化历史节流、告警配置共享和重连风暴下的 token 验证。
- 拆分 Registry、Prometheus renderer、认证路由和节点面板等高水位模块。

### 修复
- 修复浏览器增量消息的 revision 过滤、认证过期处理和 WebSocket 运行时消息校验。
- 修复结构化 token 过期通知、告警投递关闭排空、最近登录排除当前会话及登录审计记录。
- 限制在线 ipwhois 缓存规模，避免长时间运行时无界增长。

### 安全
- GitHub Actions 固定到 commit SHA，并增加 workflow guard、依赖安全覆盖和文档演示凭证扫描。
- 清理 SMTP 认证缓冲区，更新安全公告涉及的依赖，并补充 FFI 安全边界说明。

## [v3.0.1] - 2026-06-09

### 新增
- GeoIP 默认启用在线 `ipwhois` provider，可根据 Agent 的公网连接 IP 解析国家、地区和城市信息。
- 支持节点位置手动覆盖，以及 DB-IP 数据库版本变化后的刷新。

### 变更
- 安装器和 Web UI 更新检查默认只选择稳定版本，预发布版本需要显式指定。
- 服务端升级会补齐现有配置并在 Agent upgrade 命令中保留配置路径。

### 修复
- 修复 DB-IP attribution 在非 DB-IP provider 下仍显示的问题。
- 改善图表 hover 与小数坐标轴显示，并增强安装器失败阶段的诊断信息。

## [v2.3.0] - 2026-06-08

### 新增
- 节点设置支持编辑续费元数据，节点地图新增 hover 详情卡。
- Dashboard 展示服务元数据和 load sparkline，节点设置页可加载并展示 token 信息。

### 变更
- 节点监控图表直接使用当前快照作为实时基线，更新与 2FA 对话框统一适配明暗主题。
- 更新 README 中的 Web UI 截图以匹配 Vue SPA 正式界面。

### 修复
- 修复图表弹窗主题、load sparkline 数据源和节点 token 信息首屏加载问题。

## [v2.3.0-rc.1] - 2026-06-08

### 新增
- Agent 新增网络丢包率上报，服务端将 load average 持久化到历史数据。
- 服务端新增 GeoIP 支持、JSON 日志输出和包含 degraded 原因的 `/readyz` 响应。
- 新增 wire message fuzz smoke targets 与 Registry 常驻内存基准。

### 变更
- 重设计 Dashboard、节点概览、网络、硬件、设置和告警页面，统一明暗主题与移动端布局。
- Registry 拆分持久化与运行态职责；macOS 采集器拆分模块并把阻塞采集移入 blocking pool。
- 历史查询允许并发读取，Prometheus 详细节点指标可按需关闭。

### 修复
- 修复相邻 TOTP drift step、超大 Registry 文件、Web 资源响应构建和移动端地图/设置溢出。
- 修复 LAN 节点标记、节点标志推断和 macOS plist/network baseline 异常处理。

## [v2.3.0-beta.1] - 2026-06-02

### 新增
- 告警投递支持企业微信文本 webhook 和 HTML 每日巡检邮件。
- Dashboard 健康矩阵新增 load 展示。

### 变更
- 安装脚本默认拒绝预发布版本，只有显式设置 `VERSION` 时才安装 alpha/beta/rc。
- Release workflow 正确标记预发布 tag，文档建议生产环境使用最新稳定版。

### 修复
- 修复 Dashboard 桌面布局、健康矩阵、图表坐标轴、尖峰裁剪和告警设置折叠状态。
- 修复 macOS 网络接口变化后的速率基线，并增加异常速率诊断日志。

## [v2.3.0-alpha.1] - 2026-06-01

### 新增
- 完成 Vue 3 + Vite + TypeScript Web UI 迁移，引入 Pinia、Vue Router、vue-i18n、主题系统和类型化 API client。
- 新增 Dashboard、节点详情、设置、账户、告警等完整视图，以及 SVG 图表、节点地图和响应式布局。
- 新增浏览器 WebSocket 增量推送、前端 client 和端到端测试。

### 变更
- 服务端通过 `build.rs` 构建 Vue SPA，并将 `web/dist` 静态资源嵌入发布二进制。
- Release workflow 增加前端构建步骤；安装脚本支持通过 `VERSION` 选择特定版本。

### 修复
- 修复 Vue SPA 合并后的导航、布局和运行时验证问题。

## [v2.2.7] - 2026-05-28

### 新增
- 新增后台告警规则评估、Webhook/邮件投递、失败重试和每日巡检摘要。
- 新增独立的告警、账户与服务设置模型，并要求敏感告警设置修改重新认证。

### 变更
- 拆分告警投递、配置解析、启动编排、认证 helper、共享状态和历史测试模块。
- 公共 helper 使用类型化错误边界，Agent 复用跨平台采集速率计算逻辑。

### 修复
- 修复 SMTP resolved 通知配置、告警评估与投递耦合、只读认证锁持有和协议 helper panic 风险。

## [v2.2.6] - 2026-05-26

### 变更
- 更新 README 中的性能基线说明，改为基于版本记录压测结果，并补充相较上一版的直观提升摘要。
- 压缩 dashboard 与 Prometheus 默认热路径，减少 `/api/nodes` 与 `/metrics` 的响应体积和缓存 miss 开销。

### 修复
- 将 Agent 日志缓冲改为按 `node_id` 分片存储，避免全局写锁串行化所有节点日志写入。
- 缩小 overview/nodes/metrics 视图的无意义重建范围，降低高频上报下的缓存失效率。

## [v2.2.5] - 2026-05-24

### 新增
- Agent 新增 macOS 采集器支持，可在 macOS 上采集并上报系统指标。
- CI / GitHub Release 新增 macOS Agent 官方构建，覆盖 Intel 与 Apple Silicon 二进制。
- Agent 安装脚本新增 macOS `launchd` 集成，可实验性地一键安装和托管 Agent。

### 变更
- 补充 macOS Agent 从源码构建与运行的文档。
- README 更新 Agent 官方发布范围说明，明确 macOS 安装链路仍属实验性支持。
- 更新服务端 README 中的性能基线说明。
- 优化 Web UI 架构图注释的中文表达、布局平衡和标签间距。
- WebSocket 线协议升级到 v2:Agent 首帧 CPU 使用率未知时上报 `null`,Server 继续兼容 v1 Agent。

### 修复
- 稳定历史 API 负载测试，降低测试波动。
- 修复 Agent 首帧 CPU 使用率被误报为 0 的问题,聚合、历史和 Prometheus 会跳过未知 CPU 样本。
- 修复节点详情页概览和网络标签首屏图表过度简化的问题，首次进入时会拉取当前时间窗的细粒度历史数据。

## [v2.2.0] - 2026-05-23

### 新增
- 新增 GitHub Pages 快速部署指南，并在文档入口中加入跳转链接。
- CI 新增依赖漏洞扫描，提前发现高风险依赖变更。
- 暴露历史和审计写入通道的丢弃指标，便于观察背压与采样行为。

### 变更
- 拆分 WebSocket 会话流程、服务端共享状态和历史存储模块，降低核心路径复杂度。
- 文档页调整为教程式流程，并同步优化首页品牌展示、性能说明与安全指引。
- 复用 UTF-8 字节截断 helper 和 shell 参数引用 helper，减少重复实现。
- 审计日志写入改为批处理，降低写入路径开销。
- 历史写入节流移动到入队之后，采样与丢弃行为更加清晰。
- 记录历史采样和丢弃策略，补充 Rust 版本要求说明。

### 修复
- 修复审计查询中的动态 SQL 风险，改用更安全的查询构造方式。
- 修复审计写入器在查询前未完全刷新的问题。
- 修复视图缓存锁内 await 导致的潜在阻塞。
- 修复 Agent 日志缓冲区溢出裁剪需要多次扫描的问题。
- 软化 token 过期后的 Agent 重连退避，避免过密重连。
- 修复部署文档中的端口占位符转义和部署卡片图标尺寸问题。
- 移除 `install-server` 脚本对 GNU `shuf` 的依赖，提升 POSIX 环境兼容性。

### 安全
- 限制 JSON 写入请求体大小，降低资源耗尽风险。
- 加固公开认证路由，避免认证相关端点被错误暴露。
- 升级 `rustls-webpki` 到 `0.103.13`。
- 增加服务端配置序列化保护测试，防止敏感配置被直接输出。

## [v2.1.6] - 2026-05-21

### 新增
- 新增持久化审计日志，用于记录关键认证与管理操作。

### 变更
- 加固认证与历史模块的错误边界，让调用方能更明确地区分错误来源。
- Registry 增加类型化公共错误边界，减少 `anyhow` 向外层传播。
- 拆分服务端 handlers 与 settings 模块，整理测试夹具和负载测试支撑代码。
- 缩小 Registry 写入锁持有范围，并对 API 视图和 metrics 序列化结果做按 revision 缓存。

### 修复
- 使用结构化 TOML 编辑更新认证配置和 Agent token，避免字符串拼接造成配置损坏。
- 修复 registry 相关 rustfmt 回归。

### 安全
- 加固只读认证和可信代理处理。
- 扩展 WebSocket、CLI 和文件系统安全场景测试。

## [v2.1.5] - 2026-05-20

### 修复
- Agent 日志摄取丢弃现在可观测，便于排查日志背压或溢出。

### 安全
- 移除携带凭证配置类型上的 `Serialize` 派生，避免敏感配置被直接序列化。
- 统一启动流程与 settings 路由中的密码强度校验。

## [v2.1.4] - 2026-05-19

### 新增
- Web UI 新增图表详情弹窗和平均值摘要，便于查看指标趋势。

## [v2.1.3] - 2026-05-19

### 修复
- 修复 CSP 加固后仪表盘样式未正确加载的问题。

### 变更
- Release CI 拉取跨平台镜像时增加重试，降低临时网络波动导致的发布失败。

## [v2.1.2] - 2026-05-19

### 变更
- 发布流程增加验证门禁，发布前强制经过检查。

### 修复
- WebSocket 认证成功通知前先挂载控制通道，避免早期控制消息丢失。
- 修复 registry 测试中的格式化回归。

## [v2.1.1] - 2026-05-19

### 变更
- 拆分配置解析、默认值、校验和测试模块，改善 proto 配置代码的维护性。
- 拆分服务端启动逻辑、Registry 存储/token/render/test 逻辑、Agent runtime/session/config IO，以及负载测试场景。
- WebSocket 会话流程抽出更小的 helper，handlers 与 ws 中移除过早微优化。
- Registry token 轮换移除冗余参数，UI 缓存从 `Box::leak` 改为 `Arc<String>`。

### 修复
- 修复 WebSocket 计数器在 mutex poison 后的状态恢复逻辑，避免错误重置运行状态。
- 加固 Prometheus exporter 输出，避免异常指标内容破坏 metrics 格式。

### 安全
- 仪表盘页面切换到 hash-based CSP，并进一步收紧 CSP 指令。
- 加固 systemd 更新沙箱配置。

## [v2.1.0] - 2026-05-18

### 新增
- 新增 Argon2id hash/verify helper，用于节点会话 token。
- 注册节点新增 `token_hash` 与 `token_generation` 字段，同时保留 legacy `token` 读取以兼容升级。
- 新增 token 静态哈希存储覆盖测试，包括 legacy 明文 registry token 的迁移测试。
- 新增优雅 WebSocket 关闭处理，服务端关闭时会向活跃会话发送 Close frame。

### 变更
- 节点授权仅在握手阶段验证明文 token，WebSocket 热路径改用 `token_generation` 追踪状态。
- Token refresh 会轮换 token hash 并递增 generation，使既有会话能检测 registry 侧 token 变化。
- Install session 只在 bootstrap 流程中临时持有明文节点会话 token，并将明文显式写入生成的 Agent 配置。
- 历史写入改为有界 channel 和批量 writer task，避免阻塞实时 WebSocket 路径。
- 服务端的超时、ping 限制、SQLite busy timeout、输入清洗限制和 warning 间隔等调优常量改为可配置。

### 修复
- 载入 legacy registry 文件时会自动迁移明文节点 `token`：生成 `token_hash`、清空明文字段、初始化 `token_generation`，并把升级后的 registry 持久化回磁盘。
- 手动刷新和过期前刷新 token 后，WebSocket 处理会保持 generation 同步。

### 安全
- 新增或迁移后的节点会话 token 不再以明文落盘。
- 只读 Basic Auth 比较改为常量时间匹配。
- Systemd service hardening 加入更严格的 syscall filter。

## [v2.0.7] - 2026-05-18

### 新增
- 新增 Prometheus metrics endpoint（`/metrics`），便于接入监控系统。
- 新增 token refresh 并发与过期边界的测试覆盖。
- 新增 sanitize 与 registry 模块的 property-based testing。

### 修复
- 正确处理 Registry 文件锁清理时的 panic。
- 加固服务端更新临时脚本路径。

## [v2.0.6] - 2026-05-17

### 变更
- 使用 rustfmt 改进代码格式。

## [v2.0.5] - 2026-05-16

### 新增
- 新增仪表盘 API 响应缓存。
- 优化仪表盘品牌 logo 资源。

### 修复
- 缩短默认节点 token 生命周期以提升安全性。
- 对客户端隐藏上游 refresh 错误细节。
- Mutex poison 后保留有效 2FA session。
- 提升脚本的 POSIX shell 兼容性。
- 改善仪表盘首屏渲染性能。

## [v2.0.4] - 2026-05-15

### 新增
- 新增 `CONTRIBUTING.md` 贡献指南。
- 新增 `CLAUDE.md` AI 辅助开发指南。
- 新增 GitHub issue templates。
- 新增根目录 MIT license 文件。

### 变更
- 模块注释转换为 rustdoc。
- 改进代码格式。

### 安全
- 加强密码校验要求。

## [2.0.3] - 2026-05-14

### 新增
- 初始稳定版功能。

## [2.0.2] - 2026-05-13

### 新增
- Bug 修复与改进。

## [2.0.1] - 2026-05-12

### 新增
- 初始开源版本。
- Server-Agent 架构。
- WebSocket 实时通信。
- Token 认证与 TOTP 2FA。
- SQLite 历史数据存储。
- 轻量级监控（服务端 4-10MB，Agent 800KB）。

### 变更
- 主入口重构为轻量入口。
- 拆分 settings 模块，改善代码组织。

## [2.0.0] - 2026-05-11

### 新增
- 使用现代架构完成重写。
- 新增配置格式。
- 增强安全能力。

## [1.2.27] - 2026-05-10

### 修复
- 多项 bug 修复与改进。

## [1.2.26] - 2026-05-09

### 修复
- 稳定性改进。

## [1.2.25] - 2026-05-08

### 修复
- 性能优化。

## [1.2.24] - 2026-05-07

### 修复
- 安全增强。

## [1.2.23] - 2026-05-06

### 修复
- UI 改进。

## [1.2.22] - 2026-05-05

### 修复
- 配置处理改进。

## [1.2.21] - 2026-05-04

### 修复
- WebSocket 连接稳定性改进。

## [1.2.20] - 2026-05-03

### 修复
- 认证流程改进。

## [1.2.19] - 2026-05-02

### 修复
- 数据采集可靠性改进。

## [1.2.18] - 2026-05-01

### 修复
- 内存使用优化。

## [1.2.17] - 2026-04-30

### 修复
- 日志改进。

## [1.2.16] - 2026-04-29

### 修复
- 初始稳定版本。

## [1.1.0] - 2026-04-28

### 新增
- 多节点支持。
- 历史数据存储。

## [1.0.8] - 2026-04-27

### 修复
- Bug 修复与稳定性改进。

## [1.0.7] - 2026-04-26

### 修复
- 性能优化。

## [1.0.6] - 2026-04-25

### 修复
- 安全增强。

## [1.0.5] - 2026-04-24

### 修复
- UI 改进。

## [1.0.4] - 2026-04-23

### 修复
- 配置处理改进。

## [1.0.3] - 2026-04-22

### 修复
- WebSocket 稳定性改进。

## [1.0.2] - 2026-04-21

### 修复
- 认证改进。

## [1.0.1] - 2026-04-20

### 修复
- 初始 bug 修复。

## [1.0.0] - 2026-04-19

### 新增
- 初始发布。
- 基础监控功能。
- Agent-server 架构。
- 实时数据可视化。
